* cleanup old posts with the old/broken images... easy to find:

```
grep -r "[caption id=" *
```

* some work will need to be done to locate the images if we want to replace them, otherwise omit the img line
